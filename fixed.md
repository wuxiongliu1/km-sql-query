# 问题与修复记录

## DML 执行结果未显式提交

- 问题：`SqlExecutor` 使用默认非自动提交的 `SqlSession` 执行 INSERT、UPDATE、DELETE，成功后直接关闭会话，事务结果可能被回滚。
- 修复：写操作成功后显式调用 `commit()`；执行异常时显式调用 `rollback()`，并增加提交与回滚单元测试。
- 约束：后续新增写操作分支时，必须同时定义成功提交和异常回滚行为。

## 提交误带用户已暂存文件

- 问题：执行 feature commit 前只检查了工作区状态，没有隔离用户原先已暂存的文件，导致 `git commit` 将其一并纳入提交。
- 处理：识别后准备修订提交，但用户随后明确取消 Git 管理，因此停止所有 Git 操作；现有提交历史未继续改写。
- 约束：后续提交前必须检查 `git diff --cached --name-only`；存在用户预暂存内容时，使用临时索引或等效方式确保提交仅包含当前功能文件。

## 数据源配置更新后缓存未失效

- 问题：管理接口保存、更新或删除数据源配置后，没有通知 `DataSourceCache`，运行实例会继续使用旧连接池和旧连接参数。
- 修复：为数据源配置服务注入按 ID 失效的回调，在配置变更后关闭并移除对应连接池及 `SqlSessionFactory`。
- 约束：任何影响 JDBC 连接参数的数据源配置变更，都必须同步失效对应运行时缓存。

## 动态 WHERE 在运行时可能消失

- 问题：保存阶段只确认模板文本中存在 `WHERE` 或 `<where>`；当所有动态条件均为 false 时，MyBatis 最终可能生成不含 WHERE 的全表 UPDATE/DELETE。
- 修复：在执行前获取 MyBatis 渲染后的最终 SQL，忽略注释和引号内容后检查真实 WHERE 关键字，危险语句直接拒绝执行。
- 约束：DML 安全不能只校验模板源码，必须同时校验绑定实际请求参数后的最终 SQL。

## SQL 超时配置未实际生效

- 问题：设计文档约定单条 SQL 最长执行 30 秒，但动态创建的 `MappedStatement` 没有设置超时。
- 修复：为动态语句设置可配置的查询超时，配置项为 `km.sql-query.timeout-seconds`，默认 30 秒。
- 约束：对外宣称的执行限制必须落实到 JDBC/MyBatis 执行配置，不能只停留在文档中。

## 动态语句重复注册存在并发风险

- 问题：每次请求都会通过反射删除并重新注册同一个 `MappedStatement`，增加共享 MyBatis `Configuration` 的并发修改风险。
- 修复：相同 `SqlSource` 已注册时直接复用，仅在模板缓存变化时同步替换语句。
- 约束：运行时共享配置对象应以读操作为主，写入必须最小化并进行同步保护。

## 数据源密码可能通过管理接口回传

- 问题：`DatasourceConfig` 直接作为管理 API 响应对象时，会序列化数据库中保存的密码密文。
- 修复：将密码字段设置为 Jackson 只写属性，请求仍可传入，所有响应均不再输出；AES 密钥支持通过系统属性或环境变量覆盖默认兼容密钥。
- 约束：密码、令牌和密钥字段不得出现在查询或列表响应中；生产环境应配置 `KM_SQL_QUERY_AES_KEY`。

## 刷新接口与 sqlPath 前导斜杠不一致

- 问题：刷新接口按请求 URI 截取路径，单斜杠调用会得到不带 `/` 的 sqlPath，无法命中通常以 `/` 开头的配置；测试只能使用双斜杠规避。
- 修复：刷新入口统一补齐 sqlPath 的前导斜杠，并将集成测试改为正常的单斜杠 URL。
- 约束：所有入口必须对 sqlPath 使用统一的规范化格式。

## 单条 SQL 配置变更会清空全部模板缓存

- 问题：管理端修改任意一条 SQL 配置都会调用 `invalidateAll()`，导致所有动态 SQL 在后续请求中重新解析。
- 修复：缓存失效回调改为携带 sqlPath，仅清理发生变化的条目；路径重命名时同时清理新旧路径。
- 约束：配置热更新应使用最小必要的缓存失效粒度。

## 数据源缓存创建与失效存在竞态及资源泄漏窗口

- 问题：连接池创建和缓存写入分散在不同方法中，并发失效可能与创建交错；`SqlSessionFactory` 创建失败时已启动的连接池不会关闭，应用关闭时也没有统一释放缓存资源。
- 修复：使用固定条带锁串行化同一数据源的创建与失效，仅在全部初始化成功后写入缓存；失败时关闭连接池，并通过 `@PreDestroy` 统一释放全部资源。
- 约束：成组缓存对象必须原子发布，初始化失败和容器关闭都必须具备明确的资源释放路径。

## 数据源 extra 配置解析失败被静默忽略

- 问题：非法 JSON 或错误类型会被空 `catch` 吞掉，运行实例悄悄使用默认连接池参数，排障困难。
- 修复：提取扩展参数应用逻辑，校验数值类型并抛出包含数据源 ID 的明确异常。
- 约束：配置错误应快速失败并保留根因，不得静默降级为非预期默认值。

## 动态 SQL XML 解析配置不完整且序列化异常被吞掉

- 问题：模板解析只关闭了外部 DTD 加载，仍缺少 DOCTYPE、外部实体和 XInclude 的完整限制；动态节点序列化失败时返回空字符串，会悄悄改变最终 SQL。
- 修复：集中创建安全的 XML 解析器，禁用 DOCTYPE、外部实体和 XInclude；序列化开启安全处理并在失败时保留根因、立即终止解析。
- 约束：处理用户可配置 XML 时必须禁用所有外部资源能力，转换失败不得用空内容降级。

## SQL 执行器存在重复分支、重复反射和异常遮蔽

- 问题：三个写操作分支分别提交事务；每次缓存替换都重新查找反射字段；回滚再次失败时会覆盖最初的 SQL 执行异常；缓存失效失败被静默忽略。
- 修复：统一语句分发与写事务提交逻辑，启动时一次性解析 MyBatis 注册表字段；回滚失败作为 suppressed exception 附加到原异常，并记录缓存失效警告。
- 约束：清理阶段的异常不得覆盖原始业务异常；固定的反射元数据只能解析一次并应快速失败。

## SQL 类型、多语句和直接字符串替换缺少统一限制

- 问题：模板标签可以与实际 SQL 类型不一致，也可以包含多条语句或 `${}` 直接替换，从而绕过按标签实施的安全判断并扩大 SQL 注入风险。
- 修复：执行前校验模板标签与最终语句首关键字一致，拒绝多语句；默认禁止 `${}`，仅允许通过显式安全配置开启。
- 约束：安全策略必须基于最终 SQL 与声明类型共同判断，默认采用最小能力原则。

## SELECT 结果无上限且缺少统一分页

- 问题：查询使用 `selectList()` 一次性加载全部结果，数据量较大时可能造成响应膨胀和内存压力。
- 修复：统一使用 MyBatis `RowBounds` 获取至多 `max-rows + 1` 条记录；请求支持可选 `page`、`size`，响应增加 `page`、`size`、`hasMore`、`truncated` 元数据。
- 约束：任何列表查询必须有服务端硬上限，不能依赖 SQL 编写者主动添加 LIMIT。

## SQL 配置发布前无法验证真实执行效果

- 问题：管理端只能直接保存配置，缺少使用目标数据源和实际参数验证模板解析、绑定及执行结果的能力。
- 修复：新增 `POST /api/admin/sqlConfig/test`，使用唯一临时语句 ID 执行未发布模板；无论查询或写操作均回滚，并立即清理临时解析缓存和 MappedStatement。
- 约束：测试执行不得持久化 SQL 配置或业务数据，也不得污染长期运行缓存。

## 数据源缺少连接测试、运行状态和引用保护

- 问题：数据源保存前无法验证连接，运行中无法查看连接池状态；被 SQL 配置引用的数据源仍可直接删除。
- 修复：新增临时连接测试和已保存数据源健康接口，返回连通性、耗时及连接池统计；删除前检查 SQL 配置引用并拒绝破坏性操作。
- 约束：外部资源配置必须支持保存前验证和运行时健康检查，删除前必须校验引用完整性。

## SQL 接口缺少成功率、耗时和慢查询观测

- 问题：只能从单次响应看到耗时，无法按 sqlPath 聚合调用结果，也没有统一慢 SQL 日志和 Prometheus 指标出口。
- 修复：接入 Micrometer，记录 `km.sql.query.duration` 和 `km.sql.query.returned.rows`；超过阈值输出慢 SQL 警告，并通过 Actuator 暴露 health、metrics、prometheus。
- 约束：动态 SQL 接口必须具备按路径聚合的执行耗时、结果状态和返回规模指标。

## 新增引用保护测试缺少 Mockito 静态导入

- 问题：数据源引用保护用例调用了 `never()`，但首次编译时遗漏对应的 Mockito 静态导入，导致 admin 测试源码无法编译。
- 修复：补充 `org.mockito.Mockito.never` 静态导入，并重新执行完整 Maven 测试。
- 约束：新增 Mockito 验证模式后，提交前必须至少运行一次所在模块的 `testCompile` 或完整测试。
